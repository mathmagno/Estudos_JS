const nome = "Matheus"; //String
const nome1 = "Matheus"; //Sring
const num1 = 10; //number
const num2 = 10.50; // number float
let nomeAluno; // undefined = não aponta para nenhum lugar da memória!
let sobrenomeAluno = null; // Nulo -> não aponta pra local nenhuma na memória

let a = [1, 2];
const b = a;

console.log(a, b);

a = 3;
console.log(a, b);